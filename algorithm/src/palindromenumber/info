判断是不是回文数。即12321是回文数。

Pseudo code 1:
String input <- get input
char[input.length] arr = input.toCharArray();
boolean flag = false;
for(each i in char.length)
	if(char[i] == char[n-i-1])
	flag = true;
sysout(flag);

Pseudo code 2:
int input <- get input
int new = 0;
while(input/10 != 0)
	new *= 10;
	new += input%10;
	input /= 10;
if(input == new)
sysout(true);	
